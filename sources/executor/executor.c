/*
Модуль executor отвечает за выполнение команд, которые были разобраны парсером.

Основные задачи:
- Запуск внешних программ с помощью fork и execve.
- Организация пайпов между процессами, чтобы реализовать конвейеры команд (|).
- Обработка редиректов ввода/вывода (<, >, >>, <<), настройка файловых дескрипторов.
- Ожидание завершения дочерних процессов, сбор их кода возврата.
- Корректная обработка ошибок запуска, завершения и передачи данных между процессами.
- Вызов встроенных команд (builtins) в нужном контексте (основной или дочерний процесс).

Особенности:
- Executor должен уметь работать с цепочками команд, соединённых пайпами, и корректно перенаправлять потоки данных.
- Должен обеспечивать совместимость с поведением bash для всех поддерживаемых конструкций.
- Должен корректно обрабатывать коды возврата для поддержки $?.
*/
